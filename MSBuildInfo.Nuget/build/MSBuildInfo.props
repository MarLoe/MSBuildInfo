<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <Target Name="RemoveBuildInfo" />

  <Target Name="GenerateBuildInfo" />

  <PropertyGroup>
    <BuildInfoTimestamp>$([System.DateTime]::UtcNow.ToString("yyyy-MM-ddTHH:mm:ssZ"))</BuildInfoTimestamp>
  </PropertyGroup>

</Project>